<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_appointment_tree" model="ir.ui.view">
        <field name="name">hospital.appointment.tree</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <tree>
                <header>
                    <button name="action_done" string="Mark as Done" class="btn-primary"
                            type="object" />
                </header>
                <field name="name"/>
                <field name="patient_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_appointment_form" model="ir.ui.view">
        <field name="name">hospital.appointment.form</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" id="button_confirm" string="Confirm" class="btn-primary"
                            type="object" states="draft" confirm="Are you sure?"/>
                    <button name="action_done" id="button_done" string="Mark as Done" class="btn-primary"
                            type="object" states="confirmed"/>
                    <button name="action_draft" id="button_draft" string="Set to Draft" class="btn-primary"
                            type="object" states="cancel"/>
                    <button name="action_cancel" id="button_cancel" string="Cancel" type="object"
                            states="draft,done,confirmed" confirm="Are you sure?"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readolny="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="patient_id"/>
                            <field name="gender" readolny="1" force_save="1"/>
                            <field name="age"/>
                        </group>
                        <group>
                            <field name="date_appointment"/>
                            <field name="date_checkup"/>
                            <field name="note"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_appointment_search" model="ir.ui.view">
        <field name="name">hospital.appointment.search</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <search string="Patients">
                <field name="name"/>
                <field name="patient_id"/>
                <separator/>
                <group expand="1" string="Group By">
                    <filter string="Patient Name" name="patient_id" context="{'group_by': 'patient_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_hospital_appointment" model="ir.actions.act_window">
        <field name="name">Appointment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.appointment</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first appointment!
            </p>

        </field>
    </record>

    <menuitem id="menu_hospital_root" name="Hospital">
        <menuitem id="appointment_first_level_menu" name="Appointments">
            <menuitem id="appointment_model_menu_action" action="action_hospital_appointment"/>
            <menuitem id="create_appointment_model_menu_action" action="action_create_appointment"/>
        </menuitem>
    </menuitem>

</odoo>